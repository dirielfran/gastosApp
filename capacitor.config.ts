import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ccssolutions.appgastos',
  appName: 'App Gastos CCS Solutions',
  webDir: 'www'
};

export default config;
